So I used my old code for HW3 for bonus points.
Successfully implemented fork and wait.
Makefile is working.
git in comments
Added a -e getopt()

However, execv() only works b/c I hard coded.
Still need to give in fake argumnets to run.

To Run:
just make build 
./hw3 -e "ls -la"